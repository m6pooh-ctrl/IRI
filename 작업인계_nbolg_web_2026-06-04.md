# 작업 인계 문서 — IRI n blog 웹 서비스 (2026-06-04 v2)

> 다른 컴퓨터에서 Claude Code로 이어서 작업하기 위한 문서.
> 사용법: 함께 받은 ZIP을 풀고 → Claude Code에서 폴더 열기 → 이 문서를 보여주며 "이어서 하자".

---

## 0. 30초 요약

| 항목 | 내용 |
|------|------|
| 서비스 | IRI n blog — 네이버 블로그 마케팅 도구 모음 |
| 스택 | Next.js 16(App Router·Turbopack) · TypeScript · Tailwind v4 |
| 배포 | Vercel(https://iri-iota.vercel.app) — **아래 변경분은 아직 미배포** |
| GitHub | https://github.com/m6pooh-ctrl/IRI (push 권한 이슈 → 7번) |
| 데이터 | 대부분 브라우저 localStorage + 슈멤키워드는 서버 JSON |
| 실행 | `npm install` → `npm run dev` → http://localhost:3000 |
| 미리보기 | http://localhost:3000/api/dev-login (카카오 없이 입장, 개발 전용·운영 404) |
| 상태 | lint 0 · 빌드 통과 · 런타임 에러 0 |

---

## 1. 현재 기능 (사이드바)

| 메뉴 | 경로 | 설명 | 필요 키 |
|------|------|------|---------|
| 키워드 도구 | `/keyword` | 자동완성 BFS + 검색량·경쟁·문서수·비율·기회등급·IRI·황금필터 + **슈멤 뱃지** | 검색광고(필수)+오픈API(선택) |
| 슈멤키워드 | `/super` | 서버 슈멤 목록 검색 (오픈예정 뱃지) | — |
| 트렌드 | `/trend` | **최대 5개 동시 비교** + 시즌 달력 + 추천 발행 시점 | 오픈API(데이터랩) |
| 순위 추적 | `/rank` | **항상 실제 블로그탭 순위** 날짜별 기록·스파크라인 | 오픈API 불필요(블로그 주소만) |
| 경쟁글 벤치마크 | `/benchmark` | 키워드 상위노출 글 분석(제목·길이·최신성) | 오픈API |
| 서로이웃 자동화 | `/neighbor` | 데스크톱 도구 안내 (오픈예정 뱃지) | — |
| 이미지 워싱 | `/wash` | EXIF 제거 + 명암·각도·크롭 랜덤 변형(Canvas) | — |
| API 키 설정 | `/settings` | **모든 키 한 곳에서 관리** (검색광고+오픈API+블로그) | — |
| 관리자 | `/admin` | 비번 게이트 → NK키 발급 + 기능 on/off 토글 | ADMIN_PASSWORD |

> `/` (루트) 와 `/campaign` 은 `/keyword` 로 리다이렉트됨 (대시보드·체험단 제거)

---

## 2. 환경변수 (`.env.local`, ZIP 포함 — 시크릿 주의)

```
KAKAO_REST_API_KEY=...          # .env.local 파일 참고
KAKAO_CLIENT_SECRET=...         # .env.local 파일 참고
KAKAO_REDIRECT_URI=http://localhost:3000/api/auth/kakao/callback
NK_MASTER_SECRET=...            # .env.local 파일 참고
ADMIN_PASSWORD=CHANGE_ME        # 로컬 기본값. 운영은 Vercel 환경변수에서 확인
```

---

## 3. 사용자 API 키 (브라우저 localStorage에만 저장)

| 키 | 쓰는 곳 | 발급처 |
|----|---------|--------|
| 검색광고 API (Access License·Secret Key·Customer ID) | 키워드 도구 | searchad.naver.com → 도구 ▸ SA API 사용 관리 |
| 오픈API (Client ID·Secret) + 블로그 주소 | 순위 추적·벤치마크·트렌드·키워드 문서수 | developers.naver.com |

> `/settings` 페이지에서 한 번만 저장하면 모든 도구에 즉시 반영됨

---

## 4. 인증 / NK 입장키

```
/login → 카카오 → /api/auth/kakao/callback → 세션쿠키 → / (nk 없으면 /activate)
```
- 세션: httpOnly 쿠키 base64(JSON) 7일
- NK키 발급: `/admin`(비번) → `NK-YYYYMMDD-{HMAC[:12]}`
- 개발 미리보기: `/api/dev-login` (운영 404)
- 로컬 관리자 비번: `CHANGE_ME`

---

## 5. 기술 메모 (중요)

- **슈멤키워드 목록**: `src/data/super-keywords.json` (현재 빈 배열 `[]`). 엑셀 받으면 채워서 배포. API: `/api/super-list`
- **순위 추적**: 항상 실제 화면 스크래핑 (`/api/search/realrank`). 포스트 링크(BLOGID/숫자) 우선 파싱 → UA 로테이션 → 데스크톱 4회 + 모바일 2회 폴백. 첫 페이지 ~30위까지.
- **트렌드**: 다중 키워드 지원 (최대 5개). `keywords` 배열로 POST. 시즌 달력·추천 발행 시점 계산.
- **검색광고 11001(공백)**: keywordstool은 공백 거부 → enrich에서 공백 제거 후 전송. 401/403은 즉시 중단, 그 외 오류는 배치 스킵 후 계속.
- **KeyStatus 컴포넌트**: 각 도구 상단에 API 키 설정 상태 표시 + `/settings` 링크. hydration flash 방지(mounted 상태 체크).
- localStorage 로드는 useEffect(하이드레이션 방지).

---

## 6. 파일 구조 핵심

```
src/
├─ app/
│  ├─ page.tsx → redirect('/keyword')
│  ├─ keyword/ super/ trend/ rank/ benchmark/ neighbor/ wash/ admin/ login/ activate/ settings/
│  └─ api/
│     ├─ auth/(kakao,callback,me,logout,activate)  dev-login/
│     ├─ admin/(generate,verify)
│     ├─ keyword/(autocomplete, enrich, trend)
│     ├─ search/(expose[미사용], blogtop[벤치마크], realrank[실제순위])
│     └─ super-list/  ← 슈멤 목록 서빙
├─ components/
│  ├─ AppShell  Sidebar  icons
│  ├─ ApiKeyPanel  KeyStatus  ← API 키 관련 공용 컴포넌트
│  ├─ BenchmarkTool  TrendTool  RankTool  SuperKeyword  WashTool
└─ lib/
   ├─ iri.ts  session.ts  nk.ts  features.ts  apiKeys.ts  superList.ts
└─ data/
   └─ super-keywords.json  ← 슈멤 목록 (빈 배열, 엑셀 받으면 채울것)
public/ neighbor-tool.zip · help/searchad-keys.png · help/openapi-keys.png
```

---

## 7. 배포 상태 & 방법

**현재 모든 변경사항은 로컬에만 있음.**

### GitHub 권한 해결 후 push (추천)
```bash
# solmilee96가 m6pooh-ctrl/IRI Collaborator 수락 후:
git add .
git commit -m "기능 업데이트 2026-06-04"
git push origin main
# → Vercel 자동 배포 (약 2~3분)
```

### Vercel CLI로 직접 배포 (GitHub 없이)
```bash
npm install -g vercel
vercel login          # m6pooh-ctrl Vercel 계정으로 로그인
vercel --prod         # 프로젝트 폴더에서 실행 → 프로젝트 선택 → 배포
```

### 완전 새 배포 (solmilee96 단독)
1. GitHub에 새 repo 생성 (solmilee96 계정)
2. `git remote set-url origin [새 repo URL]` → push
3. vercel.com → Import Project → 새 repo 선택
4. 환경변수 `.env.local` 내용 그대로 입력
5. Deploy

---

## 8. 변경 이력 (2026-06-04 전체)

### 초기 세팅
1. FinPay 리디자인(사이드바+대시보드), 이미지 워싱, 서로이웃 안내
2. 키워드: 검색광고 키, 황금필터, 문서수·비율·기회등급, 11001 공백버그 수정
3. 슈멤키워드(제목→키워드), 트렌드(데이터랩), 순위/노출 통합, 경쟁글 벤치마크
4. 관리자 비번 게이트 + 기능 on/off
5. 삭제: AI 글도우미, 통계, 글감 발굴기

### 오늘 작업 (v2)
6. **API 키 설정 통합**: `/settings` 페이지 신설 + `ApiKeyPanel` + `KeyStatus` 컴포넌트
7. **슈멤키워드 개편**: localStorage 개인 목록 → 서버 JSON(`super-keywords.json`) 기반. 키워드 도구에 슈멤 뱃지·하이라이트·필터 추가
8. **트렌드 업그레이드**: 단일 → 최대 5개 동시 비교, 시즌 달력(히트맵), 추천 발행 시점
9. **순위 추적 개편**: 빠른 확인 탭 제거 → 항상 실제 블로그탭 순위. 키워드별 프로그레스바. "누락" → "30위 밖"
10. **realrank 개선**: 포스트 링크 우선 파싱 + UA 로테이션 + 재시도 강화 + SKIP 목록 확장
11. **코드 최적화**: enrich 배치 오류 수정, KeyStatus hydration flash 제거, superList.ts 정리, Dashboard.tsx 삭제
12. **사이드바**: 슈멤키워드·서로이웃에 "오픈예정" 뱃지. 순위/노출 → 순위 추적. 대시보드·체험단 제거
13. **관리자**: 체험단 일정 토글 제거, 설명 최신화

---

## 9. 다음 작업 후보 (백로그)

**바로 가능 (API 없음)**
- [ ] 키워드 조합기 — A단어 × B단어 조합 생성 → 바로 분석 ← 추천
- [ ] 제목 헤드라인 점수 — 글자수·키워드 포함률·감정 점수화
- [ ] UTM 파라미터 생성기 — 링크 추적용

**슈멤 목록 채우기**
- [ ] 엑셀 받으면 `src/data/super-keywords.json` 업데이트 → 배포

**API 필요**
- [ ] 트렌드: 성별/연령별 (데이터랩 한계)
- [ ] 순위: 30위 이상 (더 불안정)

---

## 10. 트러블슈팅

| 증상 | 원인 | 해결 |
|------|------|------|
| 키워드 "-"만 | 검색광고 키 미설정/오입력 | /settings 에서 저장 후 연결 테스트 |
| 분석 11001 | keywordstool 공백 거부 | enrich에서 자동 처리됨 |
| 순위 "30위 밖" | 실제 화면 첫 페이지에 없음 | 정상 동작. 30위 이상은 한계 |
| 순위 "실패" | 네이버 일시 차단/응답 지연 | 잠시 후 재시도 |
| 트렌드 오류 | 데이터랩 API 미등록 | developers.naver.com 앱 → 데이터랩 추가 |
| KeyStatus 빨간색 | API 키 미설정 | /settings 에서 설정 |
| git push 403 | 계정≠저장소소유 | 7번 참고 |
| dev ChunkLoadError | dev 켠 채 build 반복 → .next 충돌 | dev 중지 → .next 삭제 → 재시작 |

(끝 — 다음 세션에서 이 파일 보여주고 "이어서 하자")
