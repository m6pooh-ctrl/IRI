# 작업 인계 문서 — IRI n blog 웹 서비스 (2026-06-03)

> 다른 컴퓨터에서 Claude Code로 이어서 작업하기 위한 문서.
> 사용법: 이 파일과 함께 받은 ZIP을 풀고, Claude Code에서 폴더를 연 뒤 이 문서를 보여주며 "이어서 하자" 하면 됩니다.

---

## 0. 30초 요약

| 항목 | 내용 |
|------|------|
| 서비스 | IRI n blog — 네이버 블로그 마케팅 도구 모음 |
| 스택 | Next.js 16 (App Router, Turbopack) · TypeScript · Tailwind v4 |
| 배포 | Vercel (https://iri-iota.vercel.app) — **단, 아래 변경분은 아직 미배포** |
| GitHub | https://github.com/m6pooh-ctrl/IRI (push 권한 이슈 → 7번 참고) |
| 데이터 | 대부분 브라우저 localStorage (서버 DB 없음) |
| 실행 | `npm install` → `npm run dev` → http://localhost:3000 |
| 미리보기 | http://localhost:3000/api/dev-login (카카오 없이 바로 입장, 개발 전용) |

---

## 1. 현재 기능 (사이드바 메뉴)

| 메뉴 | 경로 | 설명 | 필요 키 |
|------|------|------|---------|
| 대시보드 | `/` | 체험단 건수·상태·마감 운영 지표 (FinPay 스타일) | — |
| 키워드 도구 | `/keyword` | 자동완성 BFS 확장 + 검색량·경쟁·문서수·비율·기회등급·IRI점수·황금키워드 필터 | 검색광고(필수) + 오픈API(선택, 문서수용) |
| 슈멤키워드 | `/super` | 제목 입력 → 함께 쓰면 좋을 관련 키워드 추천(검색량·황금★ 정렬) | (검색광고 있으면 지표 표시) |
| 검색 노출 확인 | `/expose` | 키워드로 내 글이 블로그검색 색인에 잡히는지 확인 | 오픈API |
| 경쟁글 벤치마크 | `/benchmark` | 키워드 상위노출 글 분석(제목·길이·최신성·블로거) | 오픈API |
| 서로이웃 자동화 | `/neighbor` | 데스크톱 도구 안내 + 실행파일 ZIP 다운로드 | — |
| 이미지 워싱 | `/wash` | EXIF 제거 + 명암·각도·크롭 랜덤 변형 (전부 브라우저 Canvas) | — |
| 체험단 일정 | `/campaign` | 체험단 관리 + 마감 D-3 알림/배지 (금액 입력은 제거됨) | — |
| 관리자 | `/admin` | **비밀번호 게이트** → NK키 발급 + 기능 on/off 토글 | ADMIN_PASSWORD |

> 기능 on/off는 관리자 페이지에서 토글 → 브라우저 localStorage(`nbolg_features`) 기반(기기별). 전체 사용자 동기화는 백엔드 필요.

---

## 2. 환경변수 (`.env.local` — ZIP에 포함되어 있음)

```
KAKAO_REST_API_KEY=1012643b40b4e0432e47eb23ee7fff68
KAKAO_CLIENT_SECRET=szauaFeUIeShlB1fnBf7maglI30fwmgZ
KAKAO_REDIRECT_URI=http://localhost:3000/api/auth/kakao/callback   # 로컬용. 운영은 https://iri-iota.vercel.app/...
NK_MASTER_SECRET=NK_MASTER_SUNU_2024
ADMIN_PASSWORD=CHANGE_ME     # 로컬 기본값. 운영은 Vercel 환경변수의 실제 값
```

> ⚠️ 이 파일엔 실제 시크릿이 들어있으니 외부 공유 주의. AI 기능 삭제로 `ANTHROPIC_API_KEY`는 제거됨.

---

## 3. 사용자가 직접 넣는 API 키 (브라우저 localStorage에만 저장)

| 키 종류 | 쓰는 곳 | 발급처 | localStorage 키 |
|---------|---------|--------|------------------|
| **네이버 검색광고 API** (Access License · Secret Key · Customer ID) | 키워드 도구(검색량·경쟁) | searchad.naver.com → 도구 ▸ SA API 사용 관리 | `nbolg_api_keys` |
| **네이버 오픈API** (Client ID · Secret) | 검색노출·벤치마크·키워드 문서수 | developers.naver.com → 앱 등록 → 사용 API에 **검색** 추가 | `nbolg_openapi` |

> 두 키는 **서로 다름**. 키워드 도구·검색노출 페이지에 **연결 테스트** 버튼 있음. 키워드 도구 [API 키 설정]에 발급 방법 스크린샷(`public/help/searchad-keys.png`) 포함.

---

## 4. 인증 / NK 입장키 흐름

```
/login → 카카오 로그인 → /api/auth/kakao/callback → 세션쿠키 set → /
세션에 nk_valid 없으면 → /activate → NK키 입력 → 검증 → 세션 업데이트 → /
```
- 세션: httpOnly 쿠키, base64(JSON), 7일.
- **NK키 발급**: `/admin`(비번 입력) → 만료일 선택 → `NK-YYYYMMDD-{HMAC[:12]}` 생성. (`NK_MASTER_SECRET`으로 서명, 로컬·운영 동일)
- **개발 미리보기**: `/api/dev-login` → 카카오 없이 nk_valid 세션 발급 (운영 빌드에선 404).

---

## 5. 기술 메모

- **IRI 점수** (`src/lib/iri.ts`): 기회(45%) + 수요(35%) + 콘텐츠(20%). 황금키워드 = 점수≥60 AND 경쟁≠높음 AND 검색량≥1000.
- **검색광고 keywordstool 11001 주의**: 키워드에 공백 있으면 거부 → enrich 라우트에서 **공백 제거 후 전송**, 결과는 원본 키워드로 매칭.
- **기회등급**: 비율(문서÷검색량) 기준 S(<2)/A(<5)/B(<10)/C(<30)/D(≥30). 문서수(오픈API) 있어야 계산됨.
- **사이드바 활성 판정**: `/campaign`은 exact 매칭(통계 삭제 후 중복활성 버그 수정).
- **localStorage 로드는 useEffect**로 (SSR 하이드레이션 미스매치 방지 — lint 룰은 eslint-disable 처리).
- 차트·이미지 처리 전부 **외부 라이브러리 없이** SVG/Canvas 직접 구현.

---

## 6. 주요 파일 구조

```
src/
├─ app/
│  ├─ page.tsx                  대시보드(서버) → Dashboard
│  ├─ keyword/ super/ expose/ benchmark/ neighbor/ wash/ campaign/  각 도구 페이지
│  ├─ admin/                    관리자(비번 게이트 + NK발급 + 기능토글)
│  ├─ login/ activate/          인증
│  └─ api/
│     ├─ auth/ (kakao, callback, me, logout, activate)
│     ├─ dev-login/             개발용 우회 로그인(운영 404)
│     ├─ admin/ (generate, verify)
│     ├─ keyword/ (autocomplete, enrich)   enrich=검색광고+오픈API문서수
│     └─ search/ (expose, blogtop)
├─ components/
│  ├─ Sidebar / AppShell / Dashboard / icons
│  ├─ SuperKeyword / ExposeTool / BenchmarkTool / WashTool
└─ lib/  (iri, campaign, session, nk, features)
public/
├─ neighbor-tool.zip            서로이웃 데스크톱 배포 ZIP(raw_key 제거됨)
└─ help/searchad-keys.png       검색광고 키 발급 안내 스크린샷
```

---

## 7. 배포 상태 & 방법 (중요)

- **현재 모든 변경은 로컬에만 있음.** 운영(vercel)엔 미반영.
- GitHub push가 막힘: 이 PC 계정은 `solmilee96`인데 저장소는 `m6pooh-ctrl` 소유 → 403.
- **해결책 (택1)**:
  1. `m6pooh-ctrl/IRI` Settings → Collaborators에 `solmilee96` 추가 → 초대 수락 → `git push`
  2. m6pooh-ctrl 계정 PAT(토큰)로 인증해 push
  3. solmilee96 소유 새 repo로 remote 교체(이 경우 Vercel 프로젝트도 재연결)
- push하면 Vercel 자동 배포. **단, AI 삭제로 ANTHROPIC_API_KEY는 Vercel에서도 지워도 됨.**
- 로컬 커밋 하나 있음(9578b85 등) — 이후 변경은 미커밋 상태일 수 있으니 `git status` 확인.

---

## 8. 이번 세션(2026-06-03) 주요 변경 이력

1. **FinPay 스타일 전면 리디자인**: 공통 사이드바(AppShell) + 운영지표 대시보드
2. **이미지 워싱**(`/wash`) 신규
3. **서로이웃 자동화 안내**(`/neighbor`) + 실행파일 ZIP 다운로드
4. **검색광고 API 키 발급 안내**(스크린샷·라벨·연결테스트) 보강
5. 캠페인 **금액 입력 제거**, 입력 글자색 라이트모드 고정
6. **황금키워드 필터** + **마감 D-3 알림**
7. **검색 노출 확인**(`/expose`) — 블로그검색 색인 확인(안전버전)
8. **문서수·비율·기회등급** 컬럼 + 오픈API 연동
9. **슈멤키워드**(`/super`) — 제목 → 함께 쓸 키워드 추천(단일화면)
10. **경쟁글 벤치마크**(`/benchmark`) 신규
11. **관리자 비번 게이트 + 기능 on/off 토글**
12. **AI 글 도우미·통계 삭제** + 죽은 코드 정리
13. **enrich 11001(공백) 버그 수정**, 전반 lint 0건·런타임 에러 0건

---

## 9. 다음에 할 수 있는 작업 (논의된 후보)

**키워드/데이터 (블랙키위 벤치마킹)**
- [ ] 키워드 클릭 → **우측 드로어 상세**: 월별 검색추이 그래프 + 성별/연령 + CTR + 포화도 (데이터랩 API 무료 추가 필요)
- [ ] CTR·클릭수 컬럼 (검색광고 API가 이미 반환하는 값)
- [ ] 순위 추적 기록(검색노출 결과 날짜별 저장 → 추이)

**인스타/콘텐츠 (논의됨, 미착수)**
- [ ] **카드뉴스/캐러셀 제작기** (텍스트 → 통일 디자인 슬라이드, Canvas) ← 추천
- [ ] 피드 그리드 플래너(3열 미리보기)
- [ ] 플랫폼별 이미지 리사이즈/크롭

**보류/제약**
- 글 재가공(규칙기반): 품질 애매(비문) → 보류. AI라면 자연스럽지만 크레딧 필요.
- 트렌드 키워드 발굴 / 통합검색 실시간 순위 / 타 계정 분석: 공식 API 없음·스크래핑(ToS) → 비권장.
- 인스타/쓰레드 예약발행·인사이트: 공식 API 가능하나 OAuth·비즈계정·앱심사로 무거움.

---

## 10. 트러블슈팅 기록

| 증상 | 원인 | 해결 |
|------|------|------|
| 키워드 분석 "-"만 뜸 | 검색광고 키 미설정/오입력 → 첫 배치 403 후 중단 | 빨간 오류배너 + 연결테스트 추가. 키 종류 구분(검색광고≠오픈API) |
| 분석 오류 11001 | keywordstool이 키워드 공백 거부 | enrich에서 공백 제거 후 전송 |
| 검색노출 "11위인데 실제론 없음" | 블로그검색 API 색인순서 ≠ 통합검색 화면순위 | "색인 N번째"로 표기 변경 + 안내문 |
| 입력 글자 흰색 | OS 다크모드 | globals.css 라이트 고정 + input 색 명시 |
| dev 로그 ChunkLoadError | dev 켠 채 build 반복 → .next 충돌 | dev 중지 → .next 삭제 → 재시작 |
| AI 503 / credit too low | Anthropic API 크레딧 부족(구독과 별개 결제) | (AI 기능은 이후 삭제됨) |
| git push 403 | 계정(solmilee96)≠저장소소유(m6pooh-ctrl) | 7번 참고 |

(끝 — 다음 세션에서 이 파일 보여주고 "이어서 하자" 하면 됩니다)
